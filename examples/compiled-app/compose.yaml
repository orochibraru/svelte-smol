services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - 3000:3000
    healthcheck:
      test: ["CMD", "/app/build/healthcheck"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 2s
